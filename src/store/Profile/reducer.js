import * as types from './actionType'

const initialState = {
    //verifyEmailLoadingStatus: false
  }
  
  export default (state = initialState, action) => {
    switch (action.type) {


      default:
        return state
    }
  }